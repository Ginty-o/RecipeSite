services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: recipe
      POSTGRES_PASSWORD: recipe
      POSTGRES_DB: recipe
    ports:
      - "5432:5432"
    volumes:
      - recipe_db:/var/lib/postgresql/data

volumes:
  recipe_db:
