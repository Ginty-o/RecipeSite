name: Keep Render Awake

on:
  schedule:
    # Runs every 10 minutes (UTC)
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS https://recipesite-1-7p9j.onrender.com/api/health
